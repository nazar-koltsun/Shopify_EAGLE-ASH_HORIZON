<div class="policy-content">
  <div class="policy-content__inner">
    {%- if page.title != blank -%}
      <h1 class="policy-content__title">{{ page.title | escape }}</h1>
    {%- endif -%}

    {%- if page.content != blank -%}
      <div class="policy-content__body">{{ page.content }}</div>
    {%- endif -%}
  </div>
</div>

{% stylesheet %}
  .section-policy-content {
    background-color: #ffffff;
  }

  .policy-content {
    width: 100%;
    background-color: #ffffff;
  }

  .policy-content__inner {
    width: 100%;
    max-width: 1440px;
    margin-inline: auto;
    padding: 60px 120px;

    @media screen and (max-width: 1300px) {
      padding: 80px 60px;
    }

    @media screen and (max-width: 749px) {
      padding: 60px 20px;
    }
  }

  .policy-content__title {
    margin: 0;
    font-family: 'Lora', serif;
    font-weight: 400;
    font-size: 36px;
    line-height: 1.25;
    color: #000000;

    @media screen and (max-width: 990px) {
      font-size: 32px;
    }

    @media screen and (max-width: 749px) {
      font-size: 28px;
    }
  }

  .policy-content__body {
    margin-block-start: 58px;
    font-family: 'Assistant', sans-serif;
    font-weight: 400;
    font-size: 16px;
    line-height: 1.6;
    color: #000000;

    @media screen and (max-width: 749px) {
      margin-block-start: 40px;
    }

    & > :first-child {
      margin-block-start: 0;
    }

    & > :last-child {
      margin-block-end: 0;
    }

    & h2,
    & h3,
    & h4,
    & h5,
    & h6 {
      margin: 0;
      margin-block-start: 1em;
      font-family: 'Assistant', sans-serif;
      font-weight: 700;
      line-height: 1.3;
      color: #000000;

    }

    & h2 {
      font-size: 16px;
    }

    & p {
      margin-block: 0 1em;
    }

    & ul,
    & ol {
      margin-block: 0 1em;
      padding-inline-start: 1.5em;
    }

    & ul.no-margin {
      margin-block: 0;
    }

    & ol.no-margin {
      margin-block: 0;
    }

    & li {
      margin-block-end: 0;

    }

    & li:last-child {
      margin-block-end: 0;
    }

    & strong,
    & b {
      font-weight: 600;
    }

    & a {
      color: inherit;
      text-decoration: underline;
      text-underline-offset: 0.15em;
    }

    & p.no-margin {
      margin-block: 0;
    }
  }
{% endstylesheet %}

{% schema %}
  {
    "name": "Policy Content",
    "class": "section-wrapper section-policy-content",
    "tag": "section",
    "settings": [],
    "presets": [
      {
        "name": "Policy Content"
      }
    ]
  }
{% endschema %}
